#
# define display information
#
name: Modify Bins in BinnedContigs
tooltip: Remove or merge Bins in BinnedContig data
screenshots: []

icon: null

#
# define a set of similar methods that might be useful to the user
#
suggestions:
    apps:
        related:
            []
        next:
            []
    methods:
        related:
            []
        next:
            []

#
# Configure the display and description of parameters
#
parameters :
    binned_contig_ref :
        ui-name : Binned Contigs
        short-hint : the binned contig data to edit
    bins_to_remove:
        ui-name : Bin to Removes
        short-hint : Enter the bin ID of a bin to remove

    new_bin_id:
        ui-name : New Bin ID
        short-hint : Enter a new ID for this merged bin
    bin_to_merge:
        ui-name : Bin to Merge
        short-hint : Enter the ID of a bin to add

    output_binned_contig_name :
        ui-name : Output BinnedContigs Name
        short-hint : select a name for the output contigs object


parameter-groups :
    bin_merges :
        ui-name : Select Bins to merge
        short-hint : Select sets of bins to merge into one
            

description : |
    <p>Basic tool for editing the bins in BinnedContig data.</p>

